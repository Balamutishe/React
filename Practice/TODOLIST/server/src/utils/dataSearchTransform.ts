export const dataSearchTransform = (queryData: {}) => {
  //   if (Object.keys(queryData).length === 1) {
  //     return {
  //       filterData: Object.keys(queryData)[0],
  //       searchData: Object.values(queryData)[0],
  //     };
  //   } else {
  //     let querySearchData = [];
  //     for (let i = 0; i < Object.keys(queryData).length; i++) {
  //       querySearchData.push({
  //         filterData: Object.keys(queryData)[i],
  //         searchData: Object.values(queryData)[i],
  //       });
  //     }
  //     return querySearchData;
  //   }
};
